import type { PublicEventImpactRecord } from "@/lib/types";

/**
 * Empty until an Entry shows a concrete impact.
 * Famous historical events are not auto-linked to diary days.
 */
export const publicEventImpactRecords: PublicEventImpactRecord[] = [];
